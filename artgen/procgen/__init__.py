"""Procedural generators (terrain, autotile, nature, buildings, water, creatures).

Populated across milestones M2-M4. Kept as a package from M1 so imports and the
CLI namespace (`python -m artgen.procgen ...`) are stable.
"""
